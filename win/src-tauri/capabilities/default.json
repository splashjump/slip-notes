{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Capability for all slip windows (main + canvas-*)",
  "windows": ["main", "canvas-*"],
  "permissions": [
    "core:default",
    "opener:default"
  ]
}
