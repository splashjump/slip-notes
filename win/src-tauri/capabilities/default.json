{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Capability for all slip windows (main + canvas-* + drag-layer + sidebar)",
  "windows": ["main", "canvas-*", "drag-layer", "sidebar"],
  "permissions": [
    "core:default"
  ]
}
