# GRILL-PLAN — 纸筏 (slip) 设计契约

> 来源：grill-me 设计会话（Q1–Q28）的完整产出，2026-08-12。
> **本文件是设计的唯一权威**；AGENTS.md 是概览并指向本文件。实施中的任何设计变更必须先改这里，再改代码。

---

## 0. 一句话与愿景

**纸筏传纸条**——一个自己会漂的纸筏（AI 化身），把你的纸条（便签）送到每一台设备。

- 便签/todo 工具，跨设备强同步（2×Win10 + 1×Android）
- 便签是唯一实体，清单项内嵌，提醒精确到清单项
- AI（pi + skill）像**第二个用户**一样操作本地便签；同步对 AI 不可见
- 愿景：AI 帮你整理便签墙、给你传纸条、接管测试；所有 AI 操作可一键撤销

## 1. 名称体系

| 用途 | 名称 |
|---|---|
| 中文产品名 | 纸筏 |
| 英文代号 | slip |
| 仓库名 | slip-notes（monorepo） |
| npm 作用域 | @slip/* |

## 2. 系统总览

- 设备：2×Win10 + 1×Android（澎湃/HyperOS）+ 阿里云 VPS（同步中枢，走 IP + 端口，免备案）
- 服务器：Node.js + TypeScript + better-sqlite3 + ws，systemd 部署
- 通信：**REST（写）+ WebSocket（Win 实时广播，心跳 30s）**；安卓纯 REST 拉取（无长连接）
- 强同步：所有设备看到相同内容；单用户
- 传输安全（v1）：明文 HTTP + token（已确认接受风险）；TLS 为后续可选加固（协议与传输层解耦，可后加）

## 3. 数据模型（单实体）

```
Note {
  id, version,              // version 由服务器签发，单调递增全局序号
  author,                   // "device:win1" / "ai" —— 历史即操作日志
  title?,                   // 可选标题
  body_type: "text" | "checklist",
  items: [{ id, text, done, urgent, time? }],   // time = 定时项
  color,                    // 便签颜色（纸感主题的分类手段）
  position: { x, y, monitor },  // 仅 PC：画布位置，跨 PC 同步
  deleted,                  // tombstone 软删除
  updated_at
}
```

- 清单项**不是实体**，是便签内容的一部分 → 勾选/修改走同一条同步与版本通道，无跨实体一致性问题
- **定时项生命周期全自动**：设置 time → 自动进入今日聚合；勾掉/删除 → 自动移除；逾期未完成 → 自动置顶 + 红色强调；本地闹钟随设置自动排程、随勾掉/删除自动取消。无手动管理入口
- 无重复提醒（留 `repeat` 扩展位，纯增量扩展）；无 snooze（v1）

## 4. 同步与版本（铁律，六条）

1. **版本号只由服务器签发**（单调递增全局序号），客户端不生产版本号——防时钟不准/陈旧设备造出"看起来更新"的旧数据
2. **陈旧写入由服务器裁决（Q29）**：写入携带 base version；base < 服务器当前 head → **接受为新版本并标记 `conflict`**（不拒绝、不阻塞同步，与 Q4 LWW 一致），响应附被跳过的版本号列表（`covered_versions`），被覆盖内容仍在服务器历史中可查看/还原。裁决只认服务器序列
3. **双线模型**：local journal（每设备只追加、**永不被 sync 覆盖**）+ 工作区（可被 sync 覆盖）——保底成立的唯一前提
4. **还原 = 写一个新版本**（新版本号，照常广播），**绝不回拨版本号**——回拨会重新打开旧覆盖新的竞态
5. **删除 = tombstone**：历史保留，任何设备可跨设备还原误删
6. **服务器保留完整版本历史**（SQLite，KB 级）——服务器是最终保底线，不能只靠客户端 journal

- 冲突：per-note LWW；输掉/被覆盖的版本打**可见标记**（"这条便签有个被覆盖的更新版本，点这里查看/还原"）——不阻塞同步，但不静默丢东西
- 断线重连：`GET /sync?since=cursor` 增量拉取（cursor = 全局变更序号）
- 服务器是唯一事实源；2Mbps 水管无压力（一条便签几 KB）

## 5. 数据传递（零机制）

- **同步即传递**：A 更新便签 → B 看到（PC 秒级 / 手机下次同步）。"省去双方打开微信的时间"
- 注意力机制 = **纯客户端**：用 updated_at 推导"最近更新的便签短暂发光"，零协议改动
- 手机无通知（极简）；widget 静默更新
- **无**推送机制、目标设备选择、已推送标记、FCM、厂商推送

## 6. 提醒

- 定时项 `time` → **每设备本地闹钟**（同步后排程），不依赖服务器调度、不依赖推送通道
- Win：卡片发光脉冲 + 边框变色 + 轻微放大，点击后恢复；紧急项常驻左侧红色条
- Android：AlarmManager 精确闹钟（`USE_EXACT_ALARM` + `setExactAndAllowWhileIdle`）；澎湃需一次性白名单（电池策略"无限制" + 允许自启动 + 锁后台可选）；**强停后闹钟失效**（安卓硬规则，直到下次打开 App）
- 过期补通知：手机下次同步时若提醒时间已过且未完成，补一条"已过期"通知（默认做）

## 7. Win 端（Tauri v2）

> **形态设计变更（2026-08-13）**：便签墙升级为**档案馆形态**（右缘边栏 + 桌面共存 + 传送门标记 + 聚合视图；互斥存在方式模型）。Tauri 细化 grill 产出 = [win/FORM-PLAN.md](win/FORM-PLAN.md)（形态先行实现计划），**验证后回填本契约**；今日虚拟卡片 → 边栏今日投影。

- 栈：Tauri（Rust 核心：数据层 + journal + 同步引擎 + localhost 接口；TS/JS UI 走 IPC；WebView2 渲染）——**Tauri 形态 = 统一接口模型的天然宿主**
- **画布窗口**（每显示器一个）：无边框透明窗口，窗口尺寸 = 该显示器便签的包围盒；窗口区域 = 便签矩形并集（`SetWindowRgn`）→ **无便签处点击天然穿透**（桌面图标、壁纸不受影响）
- 置底（`WS_EX_NOACTIVATE` + 失焦后回压 z-order）、无任务栏图标、全屏检测隐藏、多显示器支持
- 便签自由拖动，`position` 作为元数据同步到另一台 PC——两台电脑的墙一模一样
- **"今日"虚拟卡片**钉在墙面左上角：程序自动生成、聚合定时/紧急项、点击直达对应便签；不是真实便签、不同步、每台设备自己渲染
- 主题系统：卡片全部用 CSS 变量（底色/圆角/模糊/边框），**纸感先上**，毛玻璃/极简为后续皮肤，零逻辑改动
- 开机自启；点击卡片就地编辑；正文纯文本（换行 + 链接）；搜索 Ctrl+F（后续迭代）
- **开发第一步 = 画布窗口 spike**：透明 / 置底 / 区域穿透 / 多显示器 / 全屏检测五项验证；**验证失败则无损降级为网格排列**（画布保留，去掉自由拖动与位置同步，代码大部分复用）

## 8. 安卓端（Kotlin + Jetpack Compose + Glance）

- **主显示面 = 桌面 widget**：单一便签列表——顶部区块 = 未完成的定时/紧急项（全自动管理），下方 = 其余便签按更新时间倒序（刚更新的发光）；点击进 App
- App = 编辑 + 设置（非主显示面）
- 后台策略：**无前台服务**；同步触发 = 打开 App / 亮屏 / WorkManager 兜底；提醒走本地闹钟
- 澎湃白名单一次性设置（见 §6）

## 9. AI 接口（pi skill）

- **统一接口 = 本地数据层**：用户 UI 与 AI 共用同一接口，AI 像用户一样操作便签；**同步引擎在接口之下，对 AI 不可见**（用户也看不见，不需要看见）
- Win 客户端暴露 **localhost HTTP 接口**（token，绑定 127.0.0.1），按**本地语义**设计：本地序号 + `pending_sync` 标记 + 显式 `POST /sync` + journal 查询
- 服务器 API = **权威契约**（全局版本 / 陈旧写入裁决 / 全量历史 / sync cursor）——**两个表面各自诚实**，不做同构伪装（见 §15 否决记录）。REST 端点与消息格式的逐字段定义见 [docs/protocol.md](docs/protocol.md)（契约测试据此执行）
- 写入带 `author="ai"` → 版本历史天然是 AI 操作日志
- journal 支持 **batch 分组**：AI 多步操作打一个批次标记，"撤销 AI 刚才的修改" = 按批次还原，一步到位
- AI 写入**直接生效**；安全策略（如"删除要先问过用户"）写在 skill 里，不进服务器
- 交付物：`skill/SKILL.md` + 调用脚本（pi 读一遍即可操作便签）

## 10. 服务器设计

- Node.js + TypeScript + better-sqlite3（单文件、同步 API）+ ws；单进程足够（3 台设备 + AI）
- 一张表即状态 + 历史：`note(id, version, content, created_at, deleted_at)`，version = 全局单调序号
- REST 端点：CRUD（带 base version）+ 版本历史 + `sync?since=cursor`；WS：变更广播 + 心跳 30s
- 鉴权：每设备一个 token（含 AI 专用 token）
- 部署：**Docker 容器**（node:24 镜像；主机 CentOS 7 的 glibc 2.17 无法直跑新版 Node，Docker 绕开）；systemd 托管 docker 服务，容器重启策略 always
- 端口：公网监听 **50000**（高位，安全组最小放行，服务器防火墙已放）；Win 本地便签服务 **60000**（仅 127.0.0.1，AI 用 local API）

## 11. 安全与运维

- v1：明文 HTTP + token（已确认）；TLS 后续可选
- SQLite 定期快照备份（宿主机 cron，对容器数据卷快照）
- 阿里云安全组最小放行

## 12. 明确不做（v1）

重复提醒、snooze、手机推送通知、FCM/厂商推送、目标设备推送机制、多用户、Markdown、CRDT、复杂混合布局（网格+自由混排）

## 13. 实施顺序

1. **服务器 + 协议**（含契约测试——AI 即刻可接管测试）✅ 已完成上线
2. **Win 画布窗口 spike**（五项验证，失败降级网格，其余不受影响）✅ **已完成，门禁通过**（2026-08-13）
   - 透明/置底/区域穿透/全屏检测四项实测通过；多显示器代码路径就绪待实机验证（第二台 Win10 + DPI 差异）
   - 结论：**保留画布方案**（自由拖动 + 位置同步），不降级网格；实测记录与演示步骤见 [win/SPIKE.md](win/SPIKE.md)
   - 多显示器实机验证作为任务三的验收项之一，不影响方案选择
3. **Win 客户端**（**形态先行**：档案馆形态 + 动作层 + mock 数据，见 [win/FORM-PLAN.md](win/FORM-PLAN.md)；真实数据层 + journal + 同步引擎为 M5 后的任务三内容）
4. **安卓端**（Kotlin + Compose + Glance）
5. **pi skill**（SKILL.md + 调用脚本）

## 14. 决策日志（Q1–Q28）

| # | 主题 | 结论 | 关键理由 / 否决项 |
|---|---|---|---|
| Q1 | 同步中枢 | **阿里云 VPS**（走 IP 免备案） | 有公网 IP + 7×24；否决：花生壳（多一跳故障点）、公司公网 IP（合规风险） |
| Q2 | 消息形态 | **便签即消息**（后于 Q15 简化为纯同步） | 否决：独立消息频道、提醒即消息 |
| Q3 | 传输通道 | **REST（写）+ WebSocket（推）**；SSE 备选押后 | 否决：轮询（延迟地板+安卓费电）、纯 WS 双向、MQTT、FCM |
| Q4 | 冲突感知 | **混合**：LWW + 被覆盖版本可见标记 | 否决：纯静默、主动拦截（打断正常离线编辑） |
| Q5 | Win 贴桌面 | **置底贴边窗口**（半透明/无任务栏/全屏隐藏/多屏） | 否决：壁纸层穿透（交互灾难）、低存在感窗口 |
| Q6/Q7 | 内容模型 | **D2**：单实体 + 清单项级提醒 + urgent 置顶 + 今日聚合 | 否决：todo 为主（TickTick 模型）、双实体（双倍 UI 双倍同步） |
| Q8 | AI 生效方式 | **直接生效** + author 标记 + batch 撤销 | 否决：待确认队列、分级确认（阉割 AI 批量整理能力） |
| Q9/Q10 | AI 接口 | **统一接口 = 本地数据层**（AI=第二用户，同步不可见）；双表面：localhost 本地语义接口 + 服务器权威契约 | 否决：与服务器 API 同构（六处语义泄漏，见 §15） |
| Q11 | Win 栈 | **Tauri v2**（Rust 核心 + TS UI） | 否决：WPF（数据层与 UI 耦合）、Electron（200MB+ 常驻） |
| Q12 | 安卓栈 | **Kotlin + Jetpack Compose**（widget 用 Glance） | 否决：Flutter（widget/通知仍需原生，多一层引擎）、RN |
| Q13 | 安卓后台 | **A**：无前台服务；同步=打开/亮屏/WorkManager；本地闹钟 | 否决：前台服务长连接（常驻通知+耗电）、混合模式（行为不可预测） |
| Q14 | 服务器栈 | **Node.js + TypeScript** | 否决：Rust（性能伪需求）、Go（多一门语言）、Python |
| Q15 | 数据传递 | **极简**：同步即传递 + 客户端 updated_at 高亮；手机无通知 | 否决：聚合通知、任何推送机制 |
| Q16 | widget 内容 | **单一便签列表**（顶部定时/紧急项自动管理 + 下方更新时间倒序） | 否决：双 widget、只做今日聚合（纯文本便签不可见） |
| Q17/Q18 | 墙面交互 | **自由摆放** + 画布窗口方案；spike 门禁，失败降级网格 | 否决：每便签一窗口（内存爆炸）、混合布局 |
| Q19 | 视觉方向 | **主题系统（CSS 变量）**：纸感先上，毛玻璃/极简后补 | "全都要"靠主题层实现，零逻辑改动 |
| Q20 | 重复提醒 | **v1 不做**（一次性，留扩展位） | 重复场景少且常用其他工具 |
| Q21 | 设计契约 | ✅ 全部锁定 | — |
| Q22–Q26 | 命名 | **纸筏**（slip / slip-notes） | 备选：纸条、纸灵、纸精灵、纸鸢、纸船、笔仙；纸筏胜出：传递灵魂最强 + "纸筏传纸条"完整故事线 |
| Q27 | 仓库命名 | **slip-notes**，monorepo，npm 作用域 @slip/* | 否决：裸 slip（npm 全局唯一冲突）、paper-slip（代号漂移）、zhifa（两套词） |
| Q28 | 传输安全 | **明文 HTTP + token（v1）**，TLS 后续可选 | 个人数据价值低、威胁模型小、协议与传输层解耦可后加 |
| Q29 | 陈旧写入裁决 | **接受并标记 conflict**（附 covered_versions），不拒绝 | 拒绝会打断离线编辑、迫使客户端实现 rebase；接受+标记不阻塞同步、不丢数据（历史全保留），与 Q4 LWW 一致；§4 规则 2 由"拒绝或接受"敲定为"接受" |
| Q30 | 便签墙形态 | **档案馆形态**（右缘边栏 + 桌面共存 + 传送门标记 + 聚合视图），Tauri 细化 grill 产出 [win/FORM-PLAN.md](win/FORM-PLAN.md)，验证后回填 | 第二轮 grill：坞/纸堆共存、互斥存在方式（实体一次只在一处）、今日=投影、档案格自适应排列、动作层=用户/AI 统一输入抽象 |
| Q31 | 窗口穿透设计 | **废除 SetWindowRgn 精确裁剪，改 WM_NCHITTEST 命中穿透**（窗口永远全屏透明、显示层零裁剪）；保底 = 边栏收起全部 + 任务栏托盘图标恢复（**不做自动安全阀**） | 用户实测 5 类显示问题（拖动瞬移/拖出晚现/便签消失缺块/胶带被裁/落定闪跳）全部根源于 Rgn 与 DOM 异步同步的固有不同步；命中判定只影响点击归属、不影响显示，同步延迟无害；保底为手动路径（收起+托盘），拒绝自动探针（复杂且低频场景不值） |

## 15. 关键否决记录（为什么不行）

- **git/GitHub 私有仓库做同步**：git 是拉模型，无服务器→设备推送通道，核心功能（推送高亮）死亡；webhook 中转仍需常驻机器，服务器省不掉；国内访问 GitHub 稳定性反而不如自建；安卓嵌 JGit 过重。注：模型映射（commit=版本、history=journal）恰好证明了双线版本设计是成熟思路
- **local API 与服务器 API 同构**（曾主张 C 方案）：六处语义泄漏——版本身份（本地序号 vs 全局版本）、陈旧写入拒绝（本地不知道服务器真相，无法模拟裁决）、写入可见性（本地写 ≠ 全局可见，测试是假绿灯）、推送/提醒副作用、查询真相（本地 vs 权威）、历史完整性（journal 是子集）。**参与者和权威者不是同一种东西，一个接口形状覆盖两者必然撒谎**
- **每便签一个窗口**：Tauri 每窗口 = 一个 WebView2 渲染实例，30 张便签 ≈ 30 个渲染进程，内存爆炸
- **SetWindowRgn 精确裁剪（Q31 否决）**：窗口几何必须永远精确等于 DOM 几何（动画/重排/IPC/DPI 全异步）→ 固有不同步必然产生显示 bug（便签消失/缺块/装饰被裁/落定闪跳）。改 WM_NCHITTEST 命中穿透：命中矩形只决定点击归属，显示层永不裁剪（详见 Q31 与 FORM-PLAN §15）
- **安卓前台服务长连接**：常驻通知 + 耗电；且国产 ROM 白名单反正躲不掉，A 方案够用
- **目标设备推送机制**（Pushbullet 式）：数据传递最终简化成"同步 + 客户端高亮"，服务器不需要知道推送
- **CRDT**：单用户 + per-note LWW + 完整历史 + 可见标记，不需要
- **FCM / 厂商推送**：国内不可靠 / 需注册审核，个人工具不值

## 16. 术语表

| 术语 | 含义 |
|---|---|
| 纸筏 / slip | 产品名 / 英文代号 |
| note（便签） | 唯一实体 |
| item（清单项） | 便签内嵌的待办项（done/urgent/time） |
| Today（今日视图） | 定时/紧急项的聚合视图（PC 虚拟卡片 / widget 顶部区块） |
| journal | 本地只追加账本（双线的 local 线） |
| workspace（工作区） | 当前可见状态（可被 sync 覆盖） |
| local API | Win 客户端 localhost 接口（AI 用，本地语义） |
| server API | 服务器权威契约接口（全局版本/陈旧拒绝/全量历史） |
| sync cursor | 增量同步游标（全局变更序号） |
| tombstone | 软删除标记 |
| batch | journal 分组标记（"撤销 AI 的修改"按批次还原） |
